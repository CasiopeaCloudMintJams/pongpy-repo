version: 1.0
runtime: python3 
build:
  commands:
  - sudo apt-get install python3-tk
  - python -m pip install pysimplegui
    build:
      - pip install pipenv
      - pipenv install 
run: 
  command: python 4-Pong.py
